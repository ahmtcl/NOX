name: nox
description: NOX - Talk first. Connect. Then reveal.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cloud_firestore: ^5.4.4
  crypto: ^3.0.6
  firebase_app_check: ^0.3.1+6
  firebase_auth: ^5.3.1
  firebase_core: ^3.8.0
  firebase_storage: ^12.3.6
  flutter_riverpod: ^2.5.1
  go_router: ^14.6.2
  google_sign_in: ^6.2.2
  intl: ^0.20.2
  shared_preferences: ^2.3.2
  sign_in_with_apple: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
  generate: true
